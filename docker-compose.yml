services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: labq
      POSTGRES_PASSWORD: labq
      POSTGRES_DB: labq
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
